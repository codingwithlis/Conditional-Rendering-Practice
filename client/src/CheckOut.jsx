import React from "react";

const CheckOut = (props) => {
    return (
        <div>
            <p>Home Page</p>
            <button onClick={props.checkigOut}>checkout</button>
        </div>
    )
}

export default CheckOut;
